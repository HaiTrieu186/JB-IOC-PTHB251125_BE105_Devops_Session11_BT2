spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: customer-service
              uri: http://${CUSTOMER_SERVICE_HOST:localhost}:8081
              predicates:
                - Path=/api/v1/customers/**

server:
  port: 8080